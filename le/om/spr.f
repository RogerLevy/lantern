\ Sprite game object mixin

\ no idioms are established or declared; this is meant to be re-compiled as needed,
\ presumably into roles.

\ can render a bitmap or subbitmap

obj:
var bmp var ang var sx var sy var orgx var orgy var r var g var b var a var bmode
used @ to parms
